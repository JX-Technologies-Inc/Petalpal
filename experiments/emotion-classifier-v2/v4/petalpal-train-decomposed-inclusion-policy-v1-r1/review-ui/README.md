# Local blind review UI

Open `index.html` in a browser and load the frozen `../reviewer-1.csv` with the file picker. The app uses only browser localStorage; it has no network/API dependencies. Ratings remain blank until a real reviewer enters them. `Export Backup` is explicitly marked `INCOMPLETE_BACKUP`. The formal export button remains disabled until all 221 rows have Stage 2 locked.
